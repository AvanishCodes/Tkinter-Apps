import os
from tkinter import *

root = Tk()
def main():
    # get the current working directory
    cwd = os.getcwd()
    print(type(cwd))
    # cwd = 
    # formats = ('.jpg', '.jpeg', '.png', '.jfif')
    # for file in os.listdir(cwd):
        # If the file is an image
        # if os.path.splitext(file)[1].lower() in formats:
            # render it on the Tkinter screen
        # continue
    
    return

if __name__ == "__main__":
    main()